# 开发进度

本文档用于记录文件信息抽取系统的前端及后端模块化开发进度。各个模块以《前端开发计划》为基准。

## 当前开发阶段
- 进行中：**第五阶段** — 核心提取详情页 (Task Details)

## 进度明细

### 前端部分
- [x] **第一阶段**：完成并重构项目基础环境与公共组件
  - Supabase SSR 客户端（lib/supabase/client.ts、server.ts）
  - Next.js Middleware 路由守卫（middleware.ts）
  - 全局 Sidebar 用户信息显示 + 退出登录按钮
  - 系统设置页 (settings/page.tsx) — 模型配置 CRUD 及连通性测试（UI阶段，后续接后端）
- [x] **第二阶段**：用户认证与权限管理模块
  - 登录页 (/login)：连接 Supabase，表单校验、错误提示、加载态
  - 注册页 (/register)：连接 Supabase，密码一致性校验、错误提示
  - 路由守卫：未登录用户自动重定向到 /login，已登录用户访问 /login 自动跳转首页
- [x] **第三阶段**：任务与文件管理模块 (Dashboard)
  - 接入真实 Supabase 数据库表（tasks, files）
  - 实现任务列表的异步加载与统计汇总（进行中、已完成、失败数量）
  - 集成 Supabase Storage，实现真实文件夹数据挂载并联通云端
  - 完善带自定义确认弹窗的任务单项及批量删除功能
- [x] **第四阶段**：提示词组与模型配置模块
  - [x] 模型配置：连接真实 `model_configs` 数据库，支持 CRUD 及有效性校验、连通性探测。
  - [x] 提示词组：真实对接 `prompt_sets` 和 `fields`，实现动态行列编辑及数据持久化，默认提供「demo提示词组」。
  - [x] 完善交互式大模型调试组件及其后台实际通信逻辑并成功打通测试。
- [x] **第五阶段**：核心提取详情页 (Task Details)
  - [x] 左侧：文件夹树状结构展示（支持嵌套）+ 复选框（递归联动父子节点）+ 删除选中文件功能
  - [x] 中栏：文件多格式预览（PDF iframe / 图片 / TXT 文本高亮搜索 / DOCX 微软 Office Viewer）
  - [x] 右侧：词组选择下拉 + 字段列表展示 + 每字段结果手动编辑 + JSON 复制 + 文中定位（TXT 联动）
  - [x] 批量提取入口：未选中时提取全部，选中时只提取选中文件
- [~] **第六阶段**：最终联调测试与功能验收（进行中）
  - [x] FastAPI 后端初始化（main.py + CORS + 路由结构）
  - [x] 提取任务 API（POST /api/v1/extract/start + GET /api/v1/extract/results）
  - [x] LLM 服务（OpenAI 兼容协议，逐字段提取，最多重试3次，关闭 thinking，强制 JSON）
  - [x] PaddleOCR-VL-1.5 接入（异步 Job 轮询、JSONL 解析、source 匹配 bbox）
  - [x] 文件文本提取（TXT/DOCX 直接提取，PDF/图片走 OCR）
  - [x] 前端接入后端 API，提取完成后自动轮询状态并展示结果
  - [x] 文件状态指示器（左栏文件树，彩色圆点显示提取进度）
  - [x] TXT 搜索 M/N 计数 + 上下箭头切换匹配（Enter 快捷键）
  - [x] 右栏字段 ℹ tooltip 展示原文出处 (source) + 点击字段名触发文中定位 (bbox 联动)
  - [x] 预览区新增“上一个/下一个”文件导航功能，移除冗余 UI
  - [x] 结果导出 CSV（含 BOM，中文兼容 Excel）
  - [x] 后端一键启动脚本（backend/start.bat）
  - [x] 后端 .env 配置（自动填入）
  - [ ] 端到端完整流程测试

### 后端部分
- [x] FastAPI 后端框架搭建（backend/ 目录）
- [x] OCR 服务接入（PaddleOCR-VL-1.5 云端 API，ocr_service.py，含 Job 提交/轮询/解析/source 匹配）
- [x] 提取流程：PDF/图片走 OCR → LLM 提取 → bbox 写入 extraction_results
